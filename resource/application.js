/**
 * The character that should explode the array.
 *
 * @type {string}
 */
const EXPLODE_CHAR = "#";

function convertToJSON(array) {
    return JSON.stringify(array);
}

function explodeStringBy($character) {
    return $
}

generatePersonCarData();